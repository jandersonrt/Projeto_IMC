import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    boxLogo: {
        marginBottom: 40,
        alignItems: "center",
    },
    logo: {
        width: 270,
        height: 90,
    },
});

export default styles